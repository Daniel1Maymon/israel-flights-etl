# ============================================
# Israel Flights ETL - Production Environment
# ============================================
# Copy this file to .env.prod and update with real values
# cp .env.prod.example .env.prod

# ============================================
# PostgreSQL - Flights Data Database
# ============================================
POSTGRES_FLIGHTS_USER=daniel
POSTGRES_FLIGHTS_PASSWORD=change_me
POSTGRES_FLIGHTS_DB=flights_db
POSTGRES_FLIGHTS_PORT=5433
# Absolute path on host for persistent data
POSTGRES_FLIGHTS_DATA_DIR=/opt/israel-flights/data/postgres_flights

# ============================================
# ETL Configuration
# ============================================
# Schedule interval in minutes (default: 15)
SCHEDULE_INTERVAL_MINUTES=15

# CKAN API Configuration (optional, has defaults)
CKAN_BASE_URL=https://data.gov.il/api/3/action/datastore_search
CKAN_RESOURCE_ID=e83f763b-b7d7-479e-b172-ae981ddc6de5
CKAN_BATCH_SIZE=1000

# ============================================
# AWS Configuration (for S3 backups - optional)
# ============================================
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_DEFAULT_REGION=us-east-1

# Optional: enable Postgres backups to S3 after each successful ETL run
ENABLE_DB_BACKUPS=false
# S3_BUCKET_NAME=etl-flight-pipeline-bucket

# ============================================
# Application Ports
# ============================================
BACKEND_PORT=8000

# ============================================
# Logging
# ============================================
LOG_LEVEL=INFO

# ============================================
# Backend CORS Configuration
# ============================================
# Comma-separated list of allowed origins for CORS
# Update this with your Vercel frontend URL
# Include preview URLs for branch deployments: https://preview-*.vercel.app
CORS_ORIGINS=https://your-app.vercel.app,https://your-app-preview.vercel.app

# ============================================
# Railway Private Networking (ETL <-> Backend)
# ============================================
# ETL service communicates with Backend via Railway Private Network
# Backend Private URL (set in ETL service only)
# Format: http://<backend-service-name>.railway.internal:<port>
# Example: BACKEND_PRIVATE_URL=http://backend.railway.internal:8000
