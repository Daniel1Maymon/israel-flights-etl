# ============================================
# Israel Flights ETL - Production Environment
# ============================================
# Copy this file to .env.prod and update with real values
# cp .env.prod.example .env.prod

# ============================================
# PostgreSQL - Airflow Metadata Database
# ============================================
POSTGRES_AIRFLOW_USER=postgres
POSTGRES_AIRFLOW_PASSWORD=change_me
POSTGRES_AIRFLOW_DB=airflow
POSTGRES_AIRFLOW_PORT=5432
# Absolute path on host for persistent data
POSTGRES_AIRFLOW_DATA_DIR=/opt/israel-flights/data/postgres_airflow

# ============================================
# PostgreSQL - Flights Data Database
# ============================================
POSTGRES_FLIGHTS_USER=daniel
POSTGRES_FLIGHTS_PASSWORD=change_me
POSTGRES_FLIGHTS_DB=flights_db
POSTGRES_FLIGHTS_PORT=5433
# Absolute path on host for persistent data
POSTGRES_FLIGHTS_DATA_DIR=/opt/israel-flights/data/postgres_flights

# ============================================
# Airflow Configuration
# ============================================
AIRFLOW_FERNET_KEY=change_me_fernet_key
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=change_me
AIRFLOW_EMAIL=airflow@airflow.com
AIRFLOW_FIRSTNAME=Airflow
AIRFLOW_LASTNAME=Admin
AIRFLOW_WEBSERVER_PORT=8082
AIRFLOW_LOG_LEVEL=INFO

# ============================================
# AWS Configuration (for S3 access)
# ============================================
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_DEFAULT_REGION=us-east-1

# Optional: enable Postgres backups to S3 after each successful ETL run
ENABLE_DB_BACKUPS=false
# S3_BUCKET_NAME=etl-flight-pipeline-bucket

# ============================================
# Application Ports
# ============================================
BACKEND_PORT=8000

# ============================================
# Logging
# ============================================
LOG_LEVEL=INFO

# ============================================
# Backend CORS Configuration
# ============================================
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=https://israel-flights-pxuqv4mj1-daniel1maymons-projects.vercel.app/
